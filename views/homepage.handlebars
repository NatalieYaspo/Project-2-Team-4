{{#each products as |product| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2> {{product.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="project:{{project.name}}">{{project.description}}</a></h3>
  </div>
</div>
{{/each}}