{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2> {{post.title}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="project:{{post.title}}">{{post.description}}</a></h3>
  </div>
</div>
{{/each}}